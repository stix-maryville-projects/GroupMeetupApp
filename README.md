```
Some working bugs in panel navigation, including the routing from the chat panel (which leads to a blank page) and some of the buttons (which work within different borders than intended). 
Both will be fixed early this week to start beta testing and implementation of the backend.
```
